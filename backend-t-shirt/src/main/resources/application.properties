spring.application.name=backend-t-shirt

# Active Profile (Default: prod for production)
# Override with: -Dspring.profiles.active=dev for local development

spring.profiles.active=dev

# Server configuration
server.port=8080
